# Bailu CLI 环境变量配置示例
# 复制此文件为 .env 并填入你的配置

# 白鹿 API Key（必需）
BAILU_API_KEY=sk-your-api-key-here

# API 基础 URL（可选，默认：https://bailucode.com/openapi/v1）
# BAILU_BASE_URL=https://bailucode.com/openapi/v1

# 默认模型（可选，默认：bailu-Edge）
# BAILU_MODEL=bailu-Edge

# 配置目录（可选，默认：系统配置目录）
# BAILU_CONFIG_DIR=/custom/path/to/config

# 安全模式（可选，默认：review）
# 选项：dry-run | review | auto-apply
# BAILU_MODE=review
